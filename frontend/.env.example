# Local development — copy to .env.local and fill in.
# Points the frontend at the FastAPI backend. In production (Vercel) set
# this to the deployed backend URL, e.g. https://haaris-backend.onrender.com
VITE_API_URL=http://localhost:8000
